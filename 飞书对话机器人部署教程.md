# 飞书对话机器人部署教程

让机器人能听懂你说话！在群里 @机器人 说"生成文章""看热点"就能触发。

---

## 📋 准备工作（你需要有的）

- [ ] Cloudflare 账号（免费注册）
- [ ] GitHub Token（你已经有了）
- [ ] 飞书自建应用（你已经有了：cli_aa1c3fd036b8dbe9）

---

## 🔧 第一步：注册 Cloudflare（5 分钟）

1. 打开：https://dash.cloudflare.com/sign-up
2. 用邮箱注册，填密码
3. 验证邮箱
4. 登录后进入 Dashboard

**不用绑信用卡，不用买域名，完全免费。**

---

## 🔧 第二步：获取 Cloudflare API Token（2 分钟）

1. 打开：https://dash.cloudflare.com/profile/api-tokens
2. 点 **"Create Token"**（创建令牌）
3. 往下翻找到 **"Edit Cloudflare Workers"** 模板，点 **"Use template"**
4. 权限默认就行，直接点 **"Continue to summary"**
5. 点 **"Create Token"**
6. **复制保存这个 Token**（只显示一次，弄丢了要重新建）

---

## 🔧 第三步：本地部署 Worker（我来写代码，你只需要跑命令）

### 方法 A：用 Wrangler CLI 部署（推荐，最快）

#### 1. 安装 Node.js

如果电脑上没有 Node.js，先装一下：
- 下载：https://nodejs.org/ （选 LTS 版本）
- 安装时一路下一步就行

#### 2. 打开命令行

Windows 按 `Win + R`，输入 `cmd`，回车。

#### 3. 进入项目文件夹

```
cd D:\app\myfirstproject\jinrtoutiao-wenan\toutiao-content-automation\feishu-bot-worker
```

#### 4. 登录 Cloudflare

```
npx wrangler login
```

会自动打开浏览器，点 **Authorize Wrangler** 授权。

#### 5. 部署

```
npx wrangler deploy
```

部署成功后会显示一个 URL，类似：
```
https://feishu-toutiao-bot.你的用户名.workers.dev
```

**把这个 URL 复制下来，下一步要用。**

---

## 🔧 第四步：配置 Worker 的环境变量（3 分钟）

在 Cloudflare Dashboard 里：

1. 左边菜单选 **"Workers & Pages"**
2. 点 `feishu-toutiao-bot`
3. 顶部选 **"Settings"** → 左边选 **"Variables"**
4. 在 **Environment Variables** 里点 **"Add variable"**

添加以下 4 个变量：

| 变量名 | 值 | 加密？ |
|--------|-----|--------|
| `GITHUB_TOKEN` | 你的 GitHub Token（ghp_ 开头的） | ✅ Encrypt |
| `GITHUB_REPO` | 你的仓库地址，格式：用户名/仓库名 | 不用 |
| `FEISHU_APP_ID` | 你的飞书 App ID（cli_ 开头的） | 不用 |
| `FEISHU_APP_SECRET` | 你的飞书 App Secret | ✅ Encrypt |

加完后点 **"Deploy"** 生效。

---

## 🔧 第五步：飞书应用配置事件回调（3 分钟）

1. 打开飞书开放平台：https://open.feishu.cn/app
2. 点进你的应用
3. 左边菜单选 **"事件与回调"** → **"事件配置"**
4. **请求网址（Request URL）** 填你刚才部署的 Worker URL，比如：
   ```
   https://feishu-toutiao-bot.你的用户名.workers.dev
   ```
5. 点 **"保存"**，如果配置正确会显示"验证成功"

### 添加事件订阅

在同一个页面，往下翻到 **"事件列表"**：
- 点 **"添加事件"**
- 搜索 `im.message.receive_v1`（接收消息）
- 勾选它
- 点 **"保存"** → **"确认"**

---

## 🔧 第六步：给应用加权限（2 分钟）

左边菜单选 **"权限管理"**，搜索并开通以下权限：

- `im:message` — 获取与发送单聊、群组消息
- `im:message:send_as_bot` — 以应用的身份发消息
- `im:chat` — 获取群组信息

点 **"批量申请"** 或 **"申请权限"**。

---

## 🔧 第七步：发布版本（2 分钟）

1. 左边菜单选 **"版本管理与发布"**
2. 点 **"创建版本"**
3. 版本号填 `1.0.0`
4. 更新说明填 `支持对话交互`
5. 点 **"保存"**
6. 点 **"申请发布"**
7. 如果你是管理员，直接审批通过

---

## 🎉 完成！试试效果

在飞书群里 **@你的机器人**，然后说：

- `生成文章` — 完整生成文章
- `看热点` — 只抓取热点
- `重新生成` — 重跑一遍
- `帮助` — 查看帮助

机器人会回复"收到，正在处理..."，然后几分钟后把结果发过来。

---

## ❓ 常见问题

### Q: 点了保存但验证失败？
A: 检查 Worker URL 是否正确，直接在浏览器访问一下，应该显示"✅ 飞书对话机器人运行中"。

### Q: @机器人 没反应？
A: 检查：
1. 应用是否已发布并安装到群里
2. 事件订阅里有没有加 `im.message.receive_v1`
3. Worker 的环境变量有没有配置对
4. 去 Cloudflare Workers 的日志里看看有没有报错

### Q: 回复消息失败？
A: 检查 `im:message:send_as_bot` 权限有没有开通。

---

## 📁 文件位置

- Worker 代码：`feishu-bot-worker/index.js`
- 配置文件：`feishu-bot-worker/wrangler.toml`
